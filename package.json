{
  "name": "uota-worker-preview",
  "private": true,
  "scripts": {
    "build": "bundle exec jekyll build",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
